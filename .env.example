# Optional: enables semantic ranking for search_memories and rebuild_index.
# Without this, Requirements Memory falls back to keyword and dimension search.
OPENAI_API_KEY=

# Optional: override the local data directory.
# Defaults to %USERPROFILE%\.codex\requirements-memory on Windows
# or ~/.codex/requirements-memory on macOS/Linux.
REQUIREMENTS_MEMORY_HOME=

